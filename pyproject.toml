[project]
name = "astrox-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.34.0",
]

[dependency-groups]
dev = [
    "datamodel-code-generator[http]>=0.57.0",
    "numpy>=1.24.0",
    "pytest>=9.0.3",
    "pyyaml>=6.0.3",
]
